import React from 'react';
import  Router  from './Router'
import LiquorsContainer from './LiquorsContainer'
const app = () => {
    return (
        <div>
            
           <LiquorsContainer />
        </div>
    );
};

export default app;