import React from 'react';
import LiquorsContainer from './LiquorsContainer'
const app = () => {
    return (
        <div>
            <LiquorsContainer />
        </div>
    );
};

export default app;